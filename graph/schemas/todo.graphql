type Todo {
  id: ID!
  text: String!
  done: Boolean!
  user: User!
}

input NewTodo {
  text: String!
  userId: String!
}

extend type Mutation {
  createTodo(input: NewTodo!): Todo!
}

extend type Query {
  todo(id: ID!): Todo!
  todos: [Todo!]!
}